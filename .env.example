# Pilaf Development Environment Configuration
# Copy this file to .env and adjust as needed

# Dev ports (changed from defaults to avoid conflicts with production servers)
MC_PORT=25566
RCON_PORT=25576

# Server configuration
RCON_PASSWORD=cavarest
