# Configuration for HeadlessMc Server + Mineflayer Client
server_backend: headlessmc
client_backend: mineflayer

# Server settings
server_version: "1.21.5"
rcon_host: localhost
rcon_port: 25575
rcon_password: dragon123

# HeadlessMc server settings
auto_launch: true
rcon_fallback: true

# Mineflayer client settings
mineflayer_url: http://localhost:3000
